<?php

namespace SoCli;

/**
 * Defines the Constant class.
 */
class Constants {

  /**
   * Name of the folder inside project root that contains configs.
   */
  const SO_CLI_CONFIG_FOLDER_NAME = '.so-cli';
  const SO_CLI_GLOBAL_CONFIG_FOLDER = '~/.config/so-cli';

}
